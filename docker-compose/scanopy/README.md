# CONSIDER THIS IF YOU PLAN TO INSTALL THIS SOFTWARE:

There is a security risk because of its character being a network scanner
Therefore it uses
- network_mode: host
- privileged: true
- /var/lib/docker.sock
